```markdown
`--verifier` *name*   
&nbsp;&nbsp;&nbsp;&nbsp;验证提供者。可用选项：`etherscan`、`sourcify` 和 `blockscout`。默认：`etherscan`。注意：确保在 Blockscout 主页浏览器 URL 的末尾添加 "/api?"。

`--verifier-url` *url*  
&nbsp;&nbsp;&nbsp;&nbsp;可选的验证器 URL，用于提交验证请求。  
&nbsp;&nbsp;&nbsp;&nbsp;环境变量：`VERIFIER_URL`
```